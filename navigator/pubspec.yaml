name: navigator
description: An easy yet powerful navigator for simple flutter apps.
version: 1.0.0
homepage: https://github.com/predatorx7/flutter_packages/tree/main/navigator

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4

flutter:
